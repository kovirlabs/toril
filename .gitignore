# Rust / Cargo
/target
/src-tauri/target
/src-tauri/gen/schemas

# Node / frontend
node_modules
dist
dist-ssr
*.local

# UI harness output — screenshots and Playwright scratch (the harness itself is tracked)
/.playwright-mcp/
/toril-harness-*.png
/sweep-*.png

# Editor / OS
.DS_Store
.idea/
.claude/settings.local.json

# Private working notes (not for the public repo)
MARKETING.md
